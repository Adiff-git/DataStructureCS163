

// Menu.cpp
#include "raylib.h"
#include "Menu.h"

void ShowMainMenu() {
    DrawText("Main Menu", 100, 100, 30, DARKGRAY);
    // Vẽ các menu options
}

void HandleMenuInput() {
    if (IsKeyPressed(KEY_ENTER)) {
        // Xử lý khi nhấn Enter để vào màn hình chính
    }
}
